# HANDOFF — 給下一個 Claude 對話

這份文件的目的：讓一個**完全沒讀過先前對話**的 Claude，只看這份 + 專案檔案，就能理解「為什麼做到現在這樣」，並直接接手。

優先記錄**看程式碼看不出來的東西**：決策理由、走過的彎路、使用者偏好、環境地雷。
純技術細節（哪個 class 做什麼、變數叫什麼）請直接讀 `README.md` 和原始碼，那邊已經寫得很細。

---

## 0. 最近幾輪的進度

> **最新的是 0.8 + 0.9（首頁的歡迎／登入面板換成「逃生動線示範」兩顆按鈕）—— 想知道現在長什麼樣先看那兩節。**
> 0.9 是一段「改了又改回來」的模型分類，別再走一次；0.10 擺動可鎖在兩個視角之間；0.11 每條動線／通關各自的鏡頭 + 通關整棟變白；0.12 通關白色的陰影漸層；0.13 動線運鏡的時間軸；0.14 三個顏色狀態的視角分開 + 刪掉 0/1 兩頁；0.15 七個狀態的運鏡完全獨立；0.16 動線秒數依長度換算（**已被 0.17 推翻**）；0.17 鏡頭等速 + 黑幕切換（**節奏部分又被 0.18 改掉**）；0.18 點等速 + 鏡頭停半秒再轉；0.19 速度滑桿改非線性；0.20 擺動可指定順／逆時針；0.21 待機的起火點（紅點+紅煙）。
> 0.1～0.7 是上一輪：把兩頁的 3D 從「程式畫的佔位模型」換成**使用者提供的真實 BIM 模型**，加上**逃生動線**，
> 並**部署到 GitHub Pages**。細節看 `js/viewer.js`。

### 0.1 已上線（重要）
- **repo 已改成 PUBLIC**（原本 private，Pages 私有要付費；使用者選擇公開）。
- **GitHub Pages 已啟用並在跑**：<https://qifenwei03-del.github.io/fire-golden-30s/>（deploy from `main` / root）。
- **更新流程**：本機改 → `git commit` + `git push` → Pages 約 1 分鐘自動重建。
- **擋搜尋**：`robots.txt`（Disallow /）＋ `index.html` 的 `<meta name="robots" noindex>` ＋ `server.mjs` 的 `X-Robots-Tag`。Pages 上真正有效的是 **meta noindex**（子路徑的 robots.txt 爬蟲不讀）。網址公開、但不被搜尋索引 —— 這是刻意的。

### 0.2 3D 模型（真實 BIM，離線壓縮過）
- 檔案：`models/tower.glb`（首頁三層，~880KB）、`models/flat.glb`（第一頁單層「切平面」，~270KB），都 Draco 壓縮。
- **原始模型在** `C:\Users\USER\Desktop\vb\`（`3層/標準層三層.gltf`、`一層切平面/切平面.gltf`…，都是 Revit 匯出、玻璃材質、幾十～兩百 MB）。
- **離線前處理管線**（腳本在 `tools/`）：`node tools/restyle-tower.mjs`（或 `restyle-flat.mjs`）依幾何把材質收斂成幾個具名材質（`wall`/`slab`/`wall-cut`/`floor`），再
  `gltf-transform optimize <restyled> <out>.glb --compress draco --texture-compress false --simplify false`。
  **材質「名稱」是離線與執行期之間唯一的橋樑**（viewer 靠 `o.material.name` 決定玻璃/白/隱藏）。
  ⚠️ `@gltf-transform/cli` 只裝在**當次 SDK 暫存區**（會消失）；要重跑先 `npm i @gltf-transform/cli`。
- **Draco 解碼器已放進專案** `models/draco/`（`js/viewer.js` 的 `DRACO_PATH='./models/draco/'`），不再依賴 unpkg CDN —— 之前「模型等五分鐘不出現」就是卡在抓 CDN 解碼器。`server.mjs` 也補了 `.wasm` MIME。

### 0.3 兩頁的視覺（都在 viewer 執行期上材質，讀 CSS 主題色）
- **首頁（tower）**：半透明玻璃牆（`_makeBlueprintMats`，opacity .28、DoubleSide、depthWrite false）+ 白色實心樓板/樓梯 + **近側兩面外牆做剖面挖掉**（`wall-cut` → 隱藏）+ 牆框線 30°。存了一個**來回擺動 ±33°** 的視角（`layout-home.json`）。
- **第一頁（flat）**：`_makeFlatMats` —— **實心不透明深色牆**（擋掉後面框線 → 乾淨）+ **淺藍框線、門檻 1°（每個轉角都畫）** + 白色樓梯 + **地板抽掉**（`floor` → 隱藏）。**固定俯視、不轉**（`layout.json` 的 view + `SCENE_VIEWS.flat.autoRotate:false` + `loadFlatModel` 把 `customViews.flat` 強制 `autoRotate:false, swing:false`）。
- 框線邊數很多（首頁牆 ~40 萬段、第一頁 ~13 萬段），是執行期 `EdgesGeometry` 算的 → **首次載入會多花 1~3 秒**（尚未離線烘焙；之後可優化）。
- 自動框視角 `_frameView` 用**外接球 + 視窗長寬比取較窄方向**，模型旋轉不會轉出畫面。

### 0.4 逃生動線（重構成「每頁一組、互相獨立」的路線集）
- 資料：`models/routes.json`（第一頁）、`models/routes-home.json`（首頁）。由 `tools/extract-routes.mjs` / `extract-home.mjs` 從 `逃生動線.gltf`（LINE_STRIP 折線，節點名 `動線01~05`）抽出座標。首頁那份已**正規化**：紅起點在上層、綠終點在地面出口（Y≈35）。
- 架構（`js/viewer.js`）：`this._routeSets = { flat, tower }`，`_buildRoutes(name, routes, {phase})` 建一組、`_pickRouteInSet(set)` 隨機挑一條。動畫迴圈遍歷每組。
- 行為：**5 條、一次只顯示一條、每 2 秒隨機換、下一條避開前兩條、載入後第一秒不出現**（`set.gate`）。起點大**紅**圓點、終點**綠**圓點，都會閃（1 秒一次）。
- **第一頁**起點顏色跟倒數時段變（`phase:true`，藍→黃→紅，紅色階段光暈放大）；**首頁**固定紅（`phase:false`，沒倒數）。
- 節奏對齊：輪播用 viewer 動畫時鐘（`set.t0`），2 秒是 1 秒閃的整數倍 → 每次換都落在同一閃爍相位。

### 0.5 倒數連動變色（第一頁）
- `countdown.js` 新增 `onPhase(idx)`；`main.js` 轉給 `viewer.setRoutePhase(idx)`（動線起點變色）**並**在 `.page--main` 掛 `data-stage`。
- `css/style.css` 用 `.page--main[data-stage]` 讓**標題「30」（`.hero__num`）和「每一秒都很關鍵」（`.side-head h2`）** 跟著 `--stage-0/1/2`（藍/黃/紅）變色。倒數大數字本來就會變（用 `--hot`）。

### 0.6 登入（**已被 0.8 取代，整個拿掉了**）
- ~~首頁登入框輸入 **`123`** → 直接進**紅色介面**（`main.js` 的 login submit）。其他名字維持原本問候。~~
- 使用者後來要求把歡迎／登入面板拿掉，換成逃生動線示範的兩顆按鈕，所以登入相關的 HTML / CSS / JS 都刪了。
  **`123` 進紅色頁這條路已經不存在**，紅色頁現在只能用鍵盤 `1` 進去。

### 0.7 這一輪的地雷 / 注意
- **預覽窗格（mcp Browser）rAF 凍結、不合成**：看不到任何「動」（旋轉、閃爍、倒數、CSS transition）。全程只能**用 JS 讀數值驗證**，視覺要**使用者親眼看**。這是反覆卡住的主因。
- **編輯模式會刪掉 `layout*.json`**（server 有 DELETE）：commit 前常發現它們被刪成 `D`，要 `git checkout -- layout.json layout-home.json` 還原再推。**layout 檔已改成進版控**（`.gitignore` 拿掉那幾行），這樣調好的視角才會跟著部署。
- **`.claude/launch.json` 是跨專案共用的**，會被別的 session 覆蓋掉。dev server（`preview_start name:"fire-golden-30s"`）起不來時，把 fire-golden-30s 的設定加回 configurations（node `.../fire-golden-30s/server.mjs 5280`，port 5280），不要動別人的。
- 紅色頁面當時是登入目標，內容是空白骨架；**這兩頁在 0.14 已經整個刪掉了**。

### 0.8 本輪：首頁改成「逃生動線示範」（歡迎／登入面板整個拿掉）

使用者的需求原文：
1. 把首頁的歡迎面板拿掉，新增「展示逃生動線」跟「切換逃生動線」兩顆按鈕
2. 一開始維持藍色；按下任一顆 → 介面變紅、隨機挑一條路線開始跑，紅點連到綠點為時兩秒；
   連到綠點時介面變綠、路線消失、顯示「恭喜通關」

做法（細節看 `README.md` 的「逃生動線示範」一節）：

- **HTML**：`.home__panel` 那整個 `<form>` 換成 `.home__route`（兩顆按鈕，`data-obj="homeRoutePlay"` /
  `homeRouteNext"`）＋ `.home__clear`（恭喜通關，蓋在整頁上、`pointer-events:none`、不是編輯物件）。
- **CSS**：刪掉歡迎面板／輸入框／登入鈕／`welcomeBreath`／`homeShake` 那一整段，
  新增 `.home__route*`（沿用階段卡那套發光框）和 `.home__clear*`。**新增 `:root[data-theme="red"]` 一整組紅色主題。**
- **JS**：`main.js` 的登入區塊整段換成 `playRouteDemo()` / `resetRouteDemo()`；
  `viewer.js` 新增 `playRoute()` / `stopRoute()` / `routeCount()` / `_drawRouteAt()`，
  動線集多了 `mode`（首頁 `manual`＝按了才播，第一頁維持 `auto`）。
- **主題是全站的**（`<html data-theme>`），所以紅／綠會連 3D 場景一起變色 —— 這是刻意的。
  另外加了 `baseTheme`：離開首頁時會切回使用者原本選的那一個，第一頁不會被留在紅色或綠色。
- 通關訊息會**一直留著**，直到再按一次按鈕或離開首頁。使用者沒指定要不要自動收，目前選擇不自動收。

順手修掉的 bug（**這個很重要，不修的話這次的功能會壞**）：
`applyTheme()` 會重建佔位場景，而 `_addPlaceholder()` 以前是把 `this.fires` / `this.routes` 直接清成 `[]`，
**連真實模型的動線虛線流動和圓點脈動一起清掉**。以前很少換主題所以沒被發現，
現在跑一次動線就會換三次主題，一定會踩到。改成用 `ph` 旗標只清佔位場景自己的那幾筆。

驗證（預覽窗格一樣不合成、rAF 不會跑，所以是**數值驗證**，見第 5 節）：
按鈕點下去 `data-theme` 立刻變 `red`、只有 1 條動線 visible、`drawRange` 從 2 → 9 → 17（跑到底）、
線頭座標剛好落在出口那一點、`onDone` 之後 `data-theme` 變 `green`、動線 0 條、`.home__clear` 拿到 `is-on`（opacity 1）、
連按 6 次「切換」挑到的索引不會連續重複、`__goto('first')` 之後主題回 `blue`。
**唯一沒驗到的是視覺**（螢幕截圖在這個環境拿不到），請使用者確認。

### 0.9 試過「依材質透不透上色」→ **最後改回原本的幾何分類版**（動線 5 秒有留下）

這一節是**繞了一圈又回到原點**的紀錄，別再走一次。

**留下來的**：`ROUTE_RUN` 2 → 5（使用者說兩秒太快）。`js/main.js`。

**做了又收回去的**：使用者先說「首頁改成這顆模型（`C:/Users/USER/Desktop/vb/3層/標準層三層.gltf`），
材質不透對應現在的白色、材質透對應現在的牆板狀態」。照做之後：

1. 那份 Revit 匯出檔在場景根部就分成兩棵子樹，節點名叫 **「透」/「不透」**，材質也剛好只有兩個
   （`Glass` 帶 `transmissionFactor:1`、另一個不透）。改寫 `restyle-tower.mjs` 成「透→wall、不透→slab」，
   兩種判斷法（子樹 / 材質）結果完全一致。
2. 結果**天花板整片變成白色實心**，使用者說「**不要亂蓋天花板**」，
   自己把天花板那 46 個節點從「不透」搬到「透」重新匯出，我再重跑一次。
3. 看過之後使用者說「**還是回到原本那版好了（樓板跟樓梯白）**」→ 
   `git checkout -- tools/restyle-tower.mjs models/tower.glb` 還原，回到**依幾何分類**那一版。

所以現在**現行版本就是 0.2 / 0.3 描述的那一套**（樓板 + 樓梯白、近側兩面外牆剖面挖掉、其餘玻璃）。

過程中值得留著的幾件事：

- ⚠️ **使用者會直接改來源檔再叫你重跑，而且看不出來**：那兩次匯出**同路徑、同檔名、
  連檔案大小都一模一樣（50,590,716 bytes）**，只有 md5 和子樹數量不同（透/不透 819/466 → 865/420）。
  **不要用檔案大小或 mtime 判斷來源換過沒有**，要看 `scenes[0].nodes` 那兩棵子樹的 children 數量。
- `@gltf-transform/cli@4.4.2` 這次裝在 **SDK 暫存區**（`scratchpad/gt`，會消失）。npm cache 裡有 tarball，
  離線也裝得起來：`npm i @gltf-transform/cli`。整條管線跑一次約 40 秒（join 佔 30 秒）。
- 三次產出的 glb bbox 完全一樣（`-16.83, 34.63, -39.4` → `1.09, 45.10, 1.05`），
  所以換模型時 **`models/routes-home.json` 的動線座標和存好的視角都不用重調** —— 先比 bbox 再決定要不要重抽。

### 0.10 擺動可以鎖在兩個視角之間（A ↔ B）

使用者問「擺幅編輯可以鎖定在某兩個視角之間嗎」，做成編輯模式工具列的 **設 A / 設 B / 清除 AB**。

- `viewer.swing` 多了 `a` / `b` 兩端。**兩端都有值就走內插模式**（`_lerpView`），
  沒有就維持原本的 `center ± amp`。舊存檔沒有這兩個欄位，行為完全不變。
- 內插是**球座標**做的：軸心直接 lerp，鏡頭的水平角、俯仰角、距離分開插，
  水平角走最短邊（±180° 收斂）。所以 A/B 的高度和遠近不一樣也可以，會邊轉邊推近。
- 時間用 `k = (1-cos(w·t))/2`，兩端自然減速；`w` 沿用「速度」滑桿，和舊的正弦擺動同一個週期。
- 存在該頁 `layout*.json` 的 `view.swingA` / `view.swingB`（各一組 `{pos,target}`）。
  `getView` / `applyView` / `_applyView` 三個地方都要記得帶上，少一個就存不進去或載不回來。
- **順手修的**：`viewer.pauseMotion(on)` —— 進「調整模型／調整軸心」時暫停自動旋轉與擺動。
  以前拖角度時鏡頭會被 autoRotate/swing 一直搶回去（水平方向根本拖不動），
  鎖 A/B 需要精準瞄準，不修沒辦法用。暫停**不寫進存檔**，只是這段期間不搶鏡頭。
- Shift + 點「設 A」＝鏡頭跳過去看那一端（順便關掉擺動，不然馬上又被拉走）。
  這個 shift 分支需要把 click event 傳進 action，所以工具列的 dispatch 改成 `actions[a](e)`。

### 0.11 每條動線／通關各自的鏡頭（分鏡）+ 通關整棟變白

使用者：「我想分別設定逃生動線 12345 和恭喜通關的鏡頭」「恭喜通關頁面可以整棟建物全部變回白色嗎」。

- **分鏡**：`viewer._shots[場景] = { route1…route5, clear }`，每個是 `{pos,target}`，
  存在 `layout-home.json` 的 `view.shots`。API：`setShot / getShot / listShots / clearShot /
  gotoShot（直接放過去，編輯預覽用）/ playShot（平順飛過去並停住）/ releaseShot（飛回去、擺動接手）`。
  飛行是 0.8 秒 smoothstep，重用擺動那支 `_lerpView`（球座標，角度／俯仰／距離一起插）。
- **停在分鏡上時 `_hold = true`**，這期間擺動和自動旋轉都不作用；`releaseShot` 落地那一刻才把
  `_hold` 關掉並重設 `swing.t0`，鎖了 A/B 的話剛好從 A 接回去，看不到跳動。
- **沒設過機位的情境要 `releaseShot`**（`main.js` 的 `shot()` helper）。
  只設一部分卻不放開的話，鏡頭會卡在上一個情境的機位不動 —— 這個實測踩過。
- **通關整棟變白**：`viewer.setAllWhite(true)`，把玻璃牆材質換成白色樓板那組
  （`--m-slab` + emissive 0.35、opacity 1、`transparent:false`、`depthWrite:true`）。
  ⚠️ 換主題時 `_tintBlueprint()` 會重讀 CSS 顏色，所以牆板的長相統一由 **`_applyWallLook()`** 決定，
  白不白的狀態才不會在 `setTheme('green')` 的時候被洗掉。
  近側外牆的剖面挖除（`wall-cut`）維持隱藏，沒有跟著變回來。

**順手修掉的老問題（很重要）**：`applyTheme()` → `_addPlaceholder()` 以前最後會呼叫 `setScene()`，
而 `setScene()` 會**重套視角**。也就是說**每換一次主題，鏡頭就被拉回存檔的位置、分鏡也被覆蓋掉**。
以前很少換主題沒人發現，現在跑一次動線要換三次主題（藍→紅→綠），一按下去鏡頭就跳。
改法：拆出 `_syncSceneVisibility()`（只切顯示、不動鏡頭），`_addPlaceholder()` 改用它；
`setScene()` 維持原本行為（切顯示 + 套視角）。

驗證（一樣是數值驗證）：挑到的動線會飛到對應機位並精準落在該座標、飛行起點就是鏡頭原本的位置
（代表沒有被主題切換拉走）、換三次主題後 6 組分鏡都還在、通關時牆板 opacity 1 / transparent false /
色值是主題的 `--m-slab`、離開首頁後變回 0.28 玻璃、沒設機位的動線不會卡住鏡頭。

### 0.12 通關的白色加上陰影漸層（`--m-shade`）

使用者看過 0.11 的「整棟變白」之後：「想要再多一個陰影色，現在看起來亮得太均勻了，可能底部不發光就更深。」
判斷「綠色介面」指的是**通關那個畫面**（整棟變白 + 綠色主題）——當時還有 `0` 那頁空白骨架容易搞混，現在已經刪了。

- 三個主題各加一個 **`--m-shade`**（藍 `#4d6a86` / 綠 `#3f7d63` / 紅 `#8a544c`）。
- 白模式改成：材質本身設**純白**，真正的顏色交給**頂點色**（依世界座標高度，
  下面 `SHADE.span`＝55% 由 `--m-shade` smoothstep 漸層到 `--m-slab`）。
  材質色不設白的話會和頂點色相乘、整棟變暗一階。
- **自發光從 .35 降到 `SHADE.emissive`＝.07**。使用者說的「底部不發光就更深」就是這個 ——
  整棟自己在發光的話，打光造成的明暗會被沖平，看起來就是「亮得很均勻」。
  平常樓板留 .35 是為了「陰影裡不要變灰」（見 3.x 的決策），白模式**刻意反過來**。
- 算好的頂點色用 `geometry.userData.shadeKey`（= 兩個顏色的 hex）快取，
  換主題才重算。第一次約 130ms，之後來回切 0ms。

⚠️ **踩到的坑**：一開始用 `new THREE.Box3().setFromObject(tower)` 算高度範圍，
結果把**逃生動線的圓點**也算進去了 —— 線頭那顆待機時停在原點（y≈0），
整個範圍就從 0 起跳，建物（y 34.7~45）全部落在漸層的白色端，**看起來完全沒有陰影**。
現在只挑材質是 `wall` / `slab` 的 mesh 來算範圍和上色（圓點也就不會被加上頂點色屬性）。
**之後要對整棟做什麼高度相關的效果，都要記得排除動線那組。**

### 0.13 動線運鏡改成「時間軸 + 關鍵影格」

使用者：「動線視角想配合行徑路去改，可以給我路線(時間)軸配合視角設定的介面嗎」。

- 分鏡從「一條動線一個機位」升級成 **`{keys:[{t,pos,target}…]}`**，`t` 是 **0~1 的進度**（不是秒，
  這樣改 `ROUTE_RUN` 不用重設）。舊的單一 `{pos,target}` 照樣讀得到，會被當成 t=0 的一個點。
- 播動線時**每一幀**用 `_keyPair()` 找出 t 落在哪兩個影格之間，再用 `_lerpView()` 內插 —
  和擺動 A/B 共用同一支球座標內插。段與段各自 smoothstep，經過關鍵點會放慢，不會有折角。
- 開頭 `SHOT_FLY` 秒會從「按下按鈕當下的鏡頭」blend 到運鏡上（`play.from`），不然一開始會硬切。
  做法是同一幀先算出運鏡上的點，再從 from 往它 lerp（呼叫兩次 `_lerpView`，便宜）。
- 編輯 UI 是工具列下面另一塊 `.eo-track`（**選到「動線N」才出現，「通關」沒有時間軸**）。
  拖軌道＝`previewRouteAt()`（動線畫到該進度 + 線頭亮點 + 鏡頭跳到該時間點），
  菱形可拖曳改時間、雙擊刪除。開著時間軸會 `pauseMotion(true)`。

踩到的三個坑：

1. **時間軸的按鈕點不動** —— `.eo-track` 是獨立的一塊 DOM，不在 `.eo-bar` 裡面，
   工具列的 click 委派吃不到。改成把 handler 存成 `barClick` 讓兩邊共用。
2. **`.is-timeline` 下錯層** —— 一開始加在 `root`（`.page--home`）上，但 `.eo-toast` 是掛在 `#app`，
   `.is-timeline .eo-toast{top:7rem}` 選不到、提示氣泡會疊在軌道上。class 要下在 `#app` 那層。
3. **`setPointerCapture` 會丟例外**（合成事件、或指標已經放開時），而且它在 handler 最前面，
   一丟整個拖曳就沒掛上去。已經包 try/catch。

驗證：三個點（0 / 0.5 / 1）設好後，拖軌道到 0.25 → 時間顯示 1.25s、動線畫到 5/15 個點、
鏡頭落在第一段的內插位置；拖到 0 / 1 精準等於兩端的鏡頭；拖菱形 0.5→0.8 位置和標記都跟著動；
雙擊和「刪除這點」都正確、刪光會把整個分鏡清掉並停用按鈕；離開編輯模式軌道消失、預覽的動線收掉。
正式播放時 `play.keys` 有 3 個點、`_hold` 為 true、`from` 是按下當時的鏡頭；沒設點的動線會 `releaseShot`。

### 0.14 三個顏色狀態的視角完全分開 + 刪掉 `0` / `1` 兩頁

使用者：「綠色自動旋轉，藍色套回上一個來回擺動的狀態和數值，這三個顏色狀態的視角幫我全部分開，然後刪除 0 和 1 的頁面」。

- **狀態的定義**：待機（藍）＝該頁存下來的視角（含擺動 A/B、擺幅、速度）；
  動線1~5（紅）＝時間軸上的關鍵影格；通關（綠）＝`shots.clear`，而且分鏡多存了一組 **`motion`**
  （autoRotate / speed / swing / swingAmp / swingA / swingB），所以綠色可以自己轉。
- 通關沒設過機位時，`main.js` 的 `GREEN_MOTION` 當預設（自轉 speed 0.3）；
  設過就以存的為準（要它不轉就把自動旋轉關掉再存）。
- **編輯 UI**：原本的「鏡頭」下拉改成「**狀態**」下拉，多了「待機（藍）」。
  切狀態＝把畫面變成那個狀態的樣子（`applyState()`），所以工具列那排控制項調的就是選到的那個狀態；
  `rememberView()` 也跟著分流：待機→`saveView`、通關→`setShot`（連 motion）、動線→只寫檔不動待機視角。
- `_override` 旗標：待機以外的狀態接管中。`releaseShot()` 靠它判斷要不要還原，
  落地才清掉。**不能用 `_hold` 判斷** —— 綠色套完 motion 之後 `_hold` 就是 false 了，
  用它判斷會導致「通關後再按一次，綠色的自轉還留著」。
- `setScene()` 會取消 `_fly` / `_hold` / `_override`：離開首頁時 `resetRouteDemo()` 起飛、
  下一行就 `setScene('flat')`，不取消的話會在第一頁繼續飛。

**踩到的坑：分鏡被舊快照蓋掉。** `getView()` 內含 `shots`，`saveView()` 會把它一起存進 `savedViews`，
而 `_applyView()`（切場景／切狀態時跑）又會從那份快照還原 `_shots` —— 於是
「設好通關的鏡頭 → 切回待機」就把剛設的分鏡洗掉了。改成：
**`_shots` 是唯一來源**，`saveView()` 存完把 `shots` 刪掉、`getSavedView()` 讀的時候才合併目前的分鏡、
`_applyView()` 不再碰 `_shots`（只有 `applyView()` 的載入路徑會寫）。

**刪頁**：`index.html` 兩個 `.page--blank` section、`style.css` 的空白頁樣式、
`editor.css` 的綠/紅參考線顏色、`main.js` 的 `PAGES` 和 `Digit0/Digit1` 都拿掉了。
`?page=` 現在只剩 `home` / `first`。

驗證：三個狀態來回切換各自的鏡頭與運鏡都正確、分鏡不會消失；
正式流程 藍(擺動 A/B 擺幅44) → 紅 → 綠(自轉 speed .3 + 整棟白) → 再按一次回紅（自轉已解除、擺動數值原封不動）；
離開首頁再回來，待機視角和分鏡都還在；按 0 / 1 不再跳頁，DOM 只剩兩個 `.page`。

### 0.15 七個狀態的運鏡完全獨立（待機 / 動線1~5 / 通關）

使用者：「紅色是五個關卡（逃生路線）隨機」「先處理運鏡邏輯，讓藍色介面／動線1~5／綠色介面可以被分開控制」。
**不顯示關卡編號、一切隨機、先不加其他 UI。**

- 補上缺的那一半：**動線也能有自己的 `motion`**（以前只有 `clear` 有）。
  七個狀態現在資料形狀一致：`{ keys?: [...], motion?: {...} }`。
- 新增 `viewer.enterState(name, blend, {defaultMotion})`，**七個狀態共用同一套判斷**：
  `keys>=2` → 回 `'keys'`（時間軸，呼叫端每幀驅動）／`keys===1` → 飛過去再套運鏡（`'shot'`）／
  只有 motion → 原地套運鏡（`'motion'`）／什麼都沒有 → 回待機（`'idle'`）。
  `playRoute()` 和通關都走這一支，行為不會再各寫一套。
- 編輯器：`rememberView()` 依狀態分流（待機→`saveView`／動線→`setShotMotion`／通關→`setShot`）。
  以前在動線上調自動旋轉是**調了沒地方存**，現在存進那條動線自己的 motion。
- **切狀態會連配色和建物外觀一起換**（`onState` 回呼，由 `main.js` 決定：動線→紅、通關→綠+整棟白、待機→原本的主題）。
  以前只換鏡頭，等於在藍色底下調綠色的構圖，看不出實際效果。離開編輯模式會切回待機。
- 單一鏡頭又沒運鏡＝定住時，順手把 `autoRotate` / `swing.on` 旗標也關掉；
  只靠 `_hold` 壓著的話，旗標會殘留上一個狀態的值，之後某處放行就會突然轉起來。

驗證：四種組合都各測一條動線 —— 時間軸(2點)→`keys`＋hold、只有運鏡→原地自轉 0.8、
單一鏡頭→飛到定點定住、什麼都沒設→飛回待機並還原擺動；
編輯器切狀態的配色（藍/紅/綠+白）與 ● 標記都正確，離開編輯模式回到藍色。

**還沒做**：使用者問過「要不要直接變成藍紅綠三個頁面」。結論是值得分（每頁能各自排版），
但**3D 不能跟著頁面走** —— canvas 現在掛在 `.page--home` 裡，切頁會搬家 + 0.42 秒淡出，
通關那段連續運鏡會斷掉。要分的話得先把 3D 抽成三頁共用的底層（`.game__stage`），頁面只放 UI。
這一輪先只做運鏡邏輯，頁面沒有動。

### 0.16 動線秒數依長度換算（固定行走速度）

使用者：「可以依照路徑的路線長拉長時間嗎，有些路很長但走太快了」。

- `ROUTE_RUN`（固定 5 秒）換成 **`ROUTE_SPEED`（每秒走幾個模型單位，6.4）** +
  `ROUTE_SECONDS` 上下限 `[3,12]`。秒數 = 長度 ÷ 速度。`ROUTE_RUN` 只留作「還沒量到長度」的後備。
- `viewer.playRoute()` 的 `duration` 現在可以給**函式** `(i, len, lens) => 秒`，挑完動線才算。
  新增 `viewer.routeLengths(name)` 回傳每條的折線總長。
- 編輯器的 `shotSeconds` 也可以給函式 `(動線索引) => 秒`，時間軸顯示的是**那一條自己的**總秒數。
- 關鍵影格存的是 **0~1 的進度**，所以改速度不用重設運鏡（只是同一點對應的秒數變了）。

⚠️ **實際量過：五條長度其實很接近**（36.2 / 41.2 / 41.6 / 43.0 / 45.6，±13%），
所以「照長度分配」本身只造成 5.7~7.1 秒的差距。使用者覺得太快，**主因是整體速度**
（原本 5 秒跑 41.5 單位 ≈ 8.3 單位/秒，如果一單位當一公尺就是短跑速度）。
所以順便把預設放慢到 6.4 單位/秒（平均約 6.5 秒），要再快／再慢就改那一個數字。

驗證：五條的秒數 6.49 / 6.44 / 5.65 / 6.71 / 7.12，換算速度都是 6.40~6.41 單位/秒；
編輯器切到動線1 / 3 / 5，時間軸最右端分別顯示 6.49s / 5.65s / 7.12s。

### 0.17 鏡頭等速運鏡 + 狀態切換改用黑幕

使用者：「我發現我是想要旋轉的速度平均，可以幫我作弊一下嗎，可能有些走比較快有些走比較慢，
讓目前的鏡頭可以轉得比較順」「切頁面（例如路線1切綠色）不要用旋轉銜接，黑掉，轉到適合的位置再出現就好」。

**注意這推翻了 0.16**：0.16 是「人等速」，0.17 改成「**鏡頭等速**」，人反而忽快忽慢。使用者要的是後者。

- 秒數改成 **鏡頭移動量 ÷ `CAM_SPEED`**（`ROUTE_SPEED` 只在沒設運鏡時當退路）。
- 時間**照鏡頭要走多遠分配**：`_camPath()` 建弧長表、`_camAt()` 反查。
  只在段與段之間分配還不夠，**段內也要**（球座標內插本身不等速）。
- 鏡頭走 **Catmull-Rom 曲線**通過關鍵影格，不是折線。折線在關鍵點有轉折，
  等速通過就是「頓一下轉向」—— 實測折角處的速度量測會掉到 2.5~8（那其實是量測截彎取直的假象，
  但**方向的不連續是真的**）。曲線走在 (水平角, 俯仰角, 距離) 空間，theta 要先解纏繞、
  phi/radius 要夾住（Catmull-Rom 會過衝）。
- 頭尾各 12% 加減速（`easeEnds`），中間完全等速。所以中段速度 = 平均 × 1/(1-0.12) ≈ 1.136 倍，
  `CAM_SPEED=15` 量到 17.05 是對的，不是 bug。
- ⚠️ **弧長表要夠密**：一開始每段 24 格（3 個影格＝48 格）不夠，重新參數化會有速度漣漪（7~18）。
  改成每段 96 格（下限 64）之後才平。建表約 10ms，只在按下按鈕時算一次。

**黑幕**：`index.html` 的 `.blackout` + `main.js` 的 `blackout(swap)`。
**只黑 3D**（使用者後來補的：「黑模型就好」）—— 黑幕放在 `.home__stage` 裡面、`position:absolute`、
`z-index:2`（canvas 沒有 z-index 所以蓋得住，舞台標籤 z-index:3 還看得到）。
標題／按鈕／標語／恭喜通關都是舞台的**兄弟節點**，不會被蓋到。
黑 `FADE_OUT`(220ms) → 趁看不見換鏡頭/配色/建物外觀 → 亮起來。`SHOT_FLY` 從 0.8 改成 **0**（直接就定位）。
CSS 的 transition 時間要和 `FADE_OUT` 對得上。

驗證（用使用者自己存的 3 影格 × 5 條動線）：改之前每段鏡頭速度 5.7~20（差 3.5 倍），
改之後五條全部 **17.0~17.1**（216 個取樣只有 1 個偏離，是曲線中點的數值雜訊、約一幀）。
秒數 4.13 / 6.57 / 6.16 / 8.23 / 8.26。黑幕：按下按鈕立刻變黑 → 黑著的時候主題 blue→red → 亮起來；
通關同樣黑著換成 green + 整棟白。

⚠️ 使用者在每條動線上都存了 `motion: {autoRotate:true, speed:-2}`，但**有兩個以上關鍵影格時 motion 會被忽略**
（鏡頭完全由時間軸驅動）。那組設定目前是無效資料，不影響行為。

### 0.18 改成「點等速 + 鏡頭在每個影格停半秒」（**又推翻 0.17 的一半**）

使用者：「我想要轉鏡頭方向的時候停半秒，然後移動點在路線內等速、不要忽快忽慢，先不急著對到鏡頭。」

這一輪的節奏第三次改。三個版本的差別記一下，不要再繞：

| 版本 | 點 | 鏡頭 |
| --- | --- | --- |
| 0.16 | 等速（照長度） | 照進度走，忽快忽慢 |
| 0.17 | 忽快忽慢 | 等速 |
| **0.18（現行）** | **等速（照長度）** | **停 → 轉 → 停** |

- 秒數回到 `長度 ÷ ROUTE_SPEED`，`CAM_SPEED` 拿掉。動線上的點 `k = 經過時間 / 總秒數`，純線性。
- 鏡頭改成一張**時間表**（`_camSchedule`）：每個關鍵影格停 `camHold` 秒，
  剩下的時間按「這一段鏡頭要走多遠」分給各段移動，每段自己 smoothstep（停→動→停不會抽動）。
  停的時間如果超過總長的七成會自動縮短，至少留三成給移動。
- 0.17 建的東西**大部分留著**：Catmull-Rom 平滑曲線、弧長表、`_camAt`（弧長→u）都還在用，
  只是驅動方式從「弧長 ∝ 時間」換成「弧長 ∝ 時間表」。`easeEnds` 目前沒人用但留著。
- `_camPath` 多存了 `keyArc[]`（每個關鍵影格在曲線上的弧長位置），時間表要靠它分段。

驗證：五條的點速度都是 **6.40 單位/秒**（等速），時間表都是
`停0.50 → 轉 → 停0.50 → 轉 → 停0.50`；停的區間內鏡頭位移最大 0.04（等於不動），
轉的區間內 0.27~3.2（每段各自加減速）；點的進度 0.1 / 0.2 / … / 1.0 完全線性。

### 0.19 速度滑桿改成非線性（擺動要能調更慢）

使用者：「擺動的速度希望可以往更慢調整」。

範圍本來就到得了慢速（±2、step 0.05），**問題是解析度**：線性刻度下 0~0.3 這段只佔 70px 裡的幾個像素，
滑一格就從 14 秒跳到 7 秒，根本調不準。所以改成**位置平方**：滑桿 -1~1，速度 = sign(x)·x²·2。

- 慢速端的解析度變高：滑桿 1.0 → 3.5s/趟、0.5 → 14s、0.25 → 56s、0.1 → 349s。
- 滑桿旁邊加了讀數（`data-f="spinOut"`），直接顯示**實際週期**：
  擺動＝`2π/(0.9·|speed|)` 秒一趟；自轉＝`60/|speed|` 秒一圈（three.js 的 `autoRotateSpeed` 定義）。
  抽象的速度數字看不出快慢，週期才有感。
- `syncBar` 要用 `spinToSlider()` 反算滑桿位置（開根號），不能直接把 speed 塞進 value。

驗證：滑桿 → 速度 → 讀數的換算正確；拖完存檔再切狀態來回，滑桿位置、速度、讀數都保持；
自轉模式讀數會換成「120s/圈」。

### 0.20 擺動可以指定順時針／逆時針

使用者：「來回擺動想新增順時針或是逆時針方向」。

- A↔B 的內插本來一律走**最短邊**（`_viewAt` 把 theta 差收斂到 ±180°）。
  現在多一個 `swing.dir`：`auto` 走最短、`cw` 強制負向、`ccw` 強制正向（去程；回程是原路折返）。
- `_viewAt(a,b,k,outPos,outTarget,dir)` 多收一個參數，只有擺動那條路徑會傳，
  **關鍵影格的運鏡曲線不受影響**（那邊有自己的解纏繞邏輯）。
- 存在 `view.swingDir`，`getView` / `applyView` / `_applyView` / `applyMotion` / `setShot(Motion)` 都帶著。
- 工具列多一顆「擺向」，按一下循環 最短 → 順時針 → 逆時針；沒鎖 A/B 時停用。

⚠️ **順／逆的定義**：three.js 的 theta 增加＝由上往下看的逆時針（`x=r·sinθ`、`z=r·cosθ`，
從 +Z 轉向 +X）。但使用者實際是從某個斜角在看，感覺可能相反 —— 按鈕提示直接寫
「轉向跟預期相反就切另一個」，不要在這上面糾結。

驗證：A 在 +X、B 在 −Z（差 90°），A→B 中點座標 最短 `[28,-28]`、順時針 `[-28,28]`（繞遠的 270°）、
逆時針 `[28,-28]`（這組的最短邊剛好就是逆時針）。按鈕循環、存檔、停用狀態都正確。

**另外修掉**：之前幾支 node 改檔腳本在多行插入時只接了 `` 沒接換行，
在 `js/viewer.js` 留下兩處「行中間有 CR」的怪行（語法還是對的，但一行擠兩個敘述）。
已用 `/(?!
)/` 掃過所有 js/css/html 補正。**之後寫這種腳本記得整行整行 splice，不要自己接 CR。**

### 0.21 待機時的起火點（紅點 + 紅色煙霧，不畫路徑）

使用者：「在藍色介面想隨機定一個路線的起始點，要紅色閃點、不要路徑、從窗戶冒出紅色煙霧」。

- `viewer.showIdleFire(name)` / `hideIdleFire` / `idleFireRoute`。掛在動線那一組的 `set.root` 底下，
  所以跟著模型走、切場景自動隱藏。位置＝隨機一條動線的 `entries[i].pts[0]`。
- 紅點沿用 `_makeDot(..., 0xff3b2a, {big:true})`，閃爍跟動線起點共用迴圈裡的 `bk`。
- 煙霧 `_makeSmoke()`：`THREE.Sprite`（不是 Points —— **PointsMaterial 沒辦法讓每顆各自淡出**，
  一個材質只有一個 opacity）。每顆有自己的相位、方向、大小，`age = (t/life + phase) % 1`，
  往上 `rise`、往外 `spread`、`opacity = sin(π·age)·max`、越飄越大。貼圖是 canvas 畫的柔邊圓點（只做一次）。
- **按下按鈕跑的就是正在冒煙那一條**（`fromIdle` → `playRoute({index})`）。從通關再按才是隨機換。
  這是我加的連結，使用者沒明講；不想要就把 `index` 那行拿掉。
- 編輯模式的 `onState`：只有「待機（藍）」顯示起火點，動線／通關收掉。

起火點都在頂樓附近（y≈43.7，建物 34.6~45.1），煙往上 7 單位大多會冒到屋頂外面，
所以「從窗戶冒出來」的感覺是靠位置本身，不是真的做了窗戶開口。

**後續（使用者說「卡通感一點試試看」）**：柔邊漸層圓點 → **卡通雲朵**。
貼圖改成六個圓的聯集剪影、邊緣硬的、兩層 alpha（外圈 0.5 / 內裡 1.0）上色後就是「深紅描邊 + 亮紅內裡」。
顆數 16 → 9（少一點大一點才有一團一團的感覺）、加了自轉、透明度改成大部分時間維持不透明、
大小前 18% 用回彈曲線「啵」一下彈出來（實測 0.19 → 2.48 再回到 2.39 再慢慢長大）。

**再後續（使用者：「形狀想調整，下小上大、下紅上透；生成的點位幫我選最靠近旋轉視角的」）**：
- 煙霧大小改成隨高度長大（`0.32 + age*1.9`，實測 0.8 → 4.0），透明度改成隨高度淡掉（`(1-age)^0.75`，0.78 → 0.10）。
- 起火點**不再隨機**：`_nearestStart()` 找離鏡頭最近的那條動線起點，動畫迴圈每 0.2 秒重挑。
  起點是動線群組的區域座標，**要 `set.root.localToWorld()` 換成世界座標**才能跟相機比。
  有遲滯（要近一成以上才換，`bestD > curD * 0.81`），不然兩點差不多時會一直跳。
  `showIdleFire(name, {index})` 有給 index 就固定不跟。

驗證：紅點座標和動線起點完全一致、顯示中的路徑 0 條、隨機 12 次五條都抽得到；
煙霧 3 秒內 y 0.11→6.28、scale 0.99→3.96、opacity 淡入淡出（0.02→0.50→0.16）；
待機標動線1 → 按下去實際跑動線1（對上）→ 起火點收掉 → 離開再回來重抽到動線2、路徑 0 條。

---

## 1. 專案是什麼

**火災黃金30秒** — 給展場／看板用的 Three.js 網頁，全螢幕、鍵盤操作、無捲軸。
設計基準 1920×1080，整站用 `rem` 隨視窗等比縮放（`html{font-size:max(8px,min(.8333vw,1.4814vh))}`）。

四個頁面，鍵盤切換：

| 鍵 | 頁面 | 狀態 |
| --- | --- | --- |
| `Esc` | 火災黃金30秒（`first`） | **已完成** |
| `Enter` | 首頁 雲端宅邸（`home`） | **已完成** |
| `E` | 版面編輯模式（first / home 兩頁都有） | 已完成 |
| `R` | 重新倒數（僅 first） | — |

**使用者的來源素材是兩張參考圖**（綠色調的火災宣導海報、綠色調的「雲端御邸」大樓登入畫面）。
現在的成品是照那兩張圖的**構圖**做的，但**主色調已經改成藍色**（見下方決策）。

---

## 2. 現在做到哪裡

兩頁都已完成並可運作。最後一輪剛做完：

1. 歡迎面板的框改成「跟第一頁階段卡亮起時同一套發光」（細框 + 亮藍漸層 + `drop-shadow`）
2. 全站粗體字調細（`900` → `700`，第一頁小標 → `600`）
3. **已推上 GitHub**：<https://github.com/qifenwei03-del/fire-golden-30s>（分支 `main`；**現在是 PUBLIC，且已上 GitHub Pages — 見上面第 0 節**）

> 註：以下第 1～ 節是「上一輪（純佔位模型時期）」寫的，色彩／面板決策仍然有效；
> 但 3D 模型與逃生動線的最新狀態以**第 0 節**為準。

> ⚠️ 最後一輪的**視覺驗證不完整**。開發伺服器一直被系統回收、headless 截圖連不上，
> 字重和面板框是直接讀 computed style 確認數值正確，但沒有親眼看過最終畫面。
> **下一個對話請先請使用者確認視覺，或自己截一張圖確認。**

---

## 3. 重要決策與理由（看程式碼看不出來的部分）

### 3.1 主色調從綠改藍
原始參考圖是綠色調。使用者說「可以把主色調改成藍色試試看嗎」，之後就一直用藍色。
**綠色沒有刪掉**，做成可切換主題：`<html data-theme="blue|green">`、`?theme=green`、`__setTheme('green')`。
3D 場景讀同一組 CSS 變數（`--m-*`），所以切主題連模型顏色一起變。

**不隨主題變動的語意色**（刻意的，不要改成跟著主色走）：
- `--amber` 警語
- `--red` 起火點
- `--exit` 安全出口的綠 —— **國際通用標示，即使主題是藍色也保持綠色**。使用者知情且接受。

### 3.2 三個時段的顏色
`--stage-0/1/2`（`:root` 裡）由倒數圓盤和右側階段卡**共用同一組**，改一個地方兩邊一起變。

原本 0-10 秒是綠色，使用者後來要求改藍：`--stage-0: var(--brand-hi)`（跟著主色走）。
黃 `#ffb42e`、紅 `#ff4a3d` 是固定值。

對應關係（使用者親自指定的分界，實測過）：剩 30–21 藍、20–11 黃、10–0 紅。

### 3.3 階段卡亮起「只有邊框亮」
使用者原本看到的是整張卡變亮，明確說**「改成邊框亮起」**。所以：
- 框線換色加粗、外加 `drop-shadow` 光暈
- 圓形 Icon、標題、說明文字換成該段顏色
- **卡片內部底色完全不變**

後來使用者又要求「文字也要變色」，才加上標題／說明的變色。**不要退回整張卡變亮。**

同一套發光做法後來也套用到首頁的歡迎面板（使用者說「想要像第一頁 1-10 那樣發光」）。

### 3.4 面板樣式：無填色 + 漸層細框 + 倒角
使用者給了一張咖啡色同心圓的參考圖，要求「內部沒有填色、外有細細的框、框線本身有漸層」。
之後又兩次要求「線再細一點」，最後定在 `--bw: .5px`（階段卡亮起 `.75px`）。

因為線細到 0.5px 會被抗鋸齒吃掉約一半濃度，`--stroke` 的透明度是**加倍後的值**，
而且最暗那段留了下限 —— 不然右下角會淡到看不見、看起來像缺一條邊。**調整時記得這件事。**

只有階段卡額外有一層薄玻璃（`--glass` + `backdrop-filter`），四格應對和底部警語維持純線框。

### 3.5 版面編輯模式的設計
使用者要「每個物件都能移動縮放」，後來陸續加了參考線、對齊均分、固定 3D 視角。

幾個刻意的設計：
- **座標存 `rem` 不存 px** → 換螢幕解析度會等比縮放
- **還沒編輯過之前，視窗一改變就自動重新量一次**（`dirty` 旗標），不會卡在舊尺寸
- **參考線跨頁共用**，顏色代表來源頁（first 青 / home 橘 / green 黃綠 / red 桃紅）。
  使用者明確要求「每一頁都出現一樣的參考線」
- **四角 = 等比縮放（文字一起放大）、四邊 = 只改框**。這是使用者說「放大縮小」時想要的
- **對齊**：選 2 個以上以選取範圍為基準，只選 1 個就對整個版面

### 3.6 版面存檔改成寫檔案
使用者反映「再次開啟不會儲存上次的更動」。實測 localStorage 機制本身是好的，
判斷是**瀏覽器把 localStorage 清掉了**（無痕視窗或關閉時清除網站資料）。

所以改成**同時存兩份**：`layout*.json`（由 `server.mjs` 的 `PUT` 寫入）+ localStorage，讀取時**檔案優先**。
好處是換瀏覽器換電腦都讀得到，搬去展場機器只要把 json 一起帶過去。

### 3.7 開機流程
使用者要求「先黃金30秒再進首頁」。實作成：開機停在 first，**倒數跑完一輪（30 秒）自動進 home**。

兩個保護（刻意的）：
- **只自動跳一次**。手動切過頁就不再干涉，不然按 `Esc` 回去又被踢走，變成回不去
- **編輯版面時不跳**，離開編輯模式後的下一輪才會

### 3.8 載入畫面
使用者說「不要讓使用者看到半完成品」。`is-booting` class **直接寫在 `<html>` 標籤上**
（不是靠 JS 加），這樣第一次繪製前就生效。等版面套用完 `main.js` 才移除。

### 3.9 兩頁共用一顆 renderer、但模型不同
使用者要求「首頁的模型改成三層疊起來（第一頁不動）」。
沒有開第二個 WebGL context，而是同一個 `Viewer` 底下建兩組場景（`flat` / `tower`），用 `visible` 切換。

### 3.10 GitHub 選 private
使用者只說「推到 github」，沒指定公開與否。
**選 private，因為公開之後收不回來**，private 改公開只要一行指令。已在回覆中告知使用者怎麼改。

`.gitignore` 排除了 `layout*.json`（編輯模式存的版面）。如果使用者想把調好的版面版控，刪掉那三行即可。

### 3.11 使用者要求「兩頁保持一致」的部分（改動時要一起改）
使用者明確要求首頁的這些元素**跟第一頁同尺寸**，實測過數值完全一致。改任一邊時記得同步：

| 元素 | 第一頁 | 首頁 | 對齊的值 |
| --- | --- | --- | --- |
| 主標題 | `.hero__zh` | `.home__name` | `3.5rem` |
| 底部四格 / 三個功能鍵 | `.act` | `.home__act` | `240×136`，圖示 `3.1rem`，標籤 `.9rem` |
| 底部條 | `.warn` | `.home__guide-slogan` | 高 41px、`.92rem`、`letter-spacing:.2em` |

`.home__act` 因為少一行說明字，是靠 `min-height:8.5rem` 補到跟 `.act` 一樣高的。

另外首頁的下方留白特意設成 `1.1rem`（跟 `.page--main` 一樣），
右下標語才會和第一頁的「火災無情」**同高**（實測 top=1022 / bottom=1062）。

### 3.12 第一頁的 DOM 結構被改過
使用者說「第一頁跑掉了，可以置中嗎」。量出來頁面外框其實是對稱的（左 42 / 右 41），
偏掉的是**四格那排**（被關在左欄裡置中，左留 198 右留 675）。

修法是**把 `.acts` 從 `.col-main` 裡搬出來**，讓 `.layout` 變成兩列：
```
"main side"
"acts acts"
```
所以 `.acts` 現在是 `.layout` 的直接子元素、橫跨兩欄。副作用是右欄變短、階段卡往上移，看起來反而更平衡。
**不要把它搬回 `.col-main`。**

### 3.13 `.phase.is-on` 用 `translate` 不用 `transform`
階段卡亮起時會左移 `.35rem`。這個位移刻意用 `translate` 屬性而不是 `transform`，
因為**編輯模式會在同一個元素上設 `transform: scale()`**，兩者會互相蓋掉。

### 3.14 彗星光帶：改方向時漸層要一起鏡射
內圈的彗星光帶目前是**逆時針、2 秒一圈**（使用者指定，中間改過 1 秒 → 5 秒 → 2 秒）。

⚠️ **改旋轉方向時，`conic-gradient` 的尾巴也必須鏡射**，
否則尾巴會跑到頭的前面變成「在推」而不是「在拖」。目前頭在 0°、尾巴往順時針方向拖。

### 3.15 最後 5 秒閃爍頻率
使用者先要 0.6 秒一次，看過之後改成**一秒一次**（跟數字每秒跳動同步）。門檻是 `createCountdown({urgentAt:5})`。

---

## 4. 走過的彎路 —— 不要重複嘗試

這一段最重要，每一條都是實際踩過的。

### CSS

| 試過但失敗的做法 | 為什麼失敗 | 現在的做法 |
| --- | --- | --- |
| `html.is-booting body{visibility:hidden}` 蓋住載入中的畫面 | `visibility` 會被子層的 `.page.is-active{visibility:visible}` 蓋回來，完全沒效果 | 改用 `opacity:0`（整個子樹一起合成，蓋得住） |
| 倒角面板 = 元素填邊框色 + `::after` 內縮填底色 | 內部要改成透明時整片透出邊框色，整張卡變亮 | `clip-path: polygon(evenodd, 外框…, 內孔…)` 畫中空環 |
| `clip-path` 下在元素本身 | 會連子元素一起裁掉，面板內容全部消失 | 一律下在偽元素（`::before` 玻璃、`::after` 框線） |
| evenodd 環只列「外框 8 點 + 內孔 8 點」 | 外圈輪廓沒閉合，**左緣那條線不會被畫出來** | 兩圈最後都要補回起點（`--ring` 裡有註解） |
| 歡迎面板框線做成跑馬燈掃光（`background-position` 動畫） | 使用者說「不太正確」 | 改成階段卡那套靜態發光 |
| 中文字加 `letter-spacing` 後直接置中 | 字距會在最後一個字後面多留一格，看起來偏左 | 用 `padding-left` / `text-indent` 補回來 |

### 編輯器

| 試過但失敗的做法 | 為什麼失敗 |
| --- | --- |
| 絕對定位的 grid 項目保留 `grid-area` | **容器塊會變成「它那一格」而不是整個容器**，`left/top` 算成相對於格子，物件飛到畫面外。修法：`.is-edited` 時容器改 `display:block` |
| 切頁動畫還沒跑完就凍結版面 | `.page` 還停在 `scale(.985)`，量到的是縮過的座標，版面整個歪。修法：`.eo-measuring{transform:none}` |
| 凍結時把 `.phase.is-on` 的 `translate` 一起量進去 | 凍結後等於位移兩次。同上，`.eo-measuring` 一併關掉 |
| `.eo-guides` 容器沒設 `pointer-events:none` | 鋪滿整層把所有物件的選取都吃掉，**編輯模式完全不能選取** |
| 參考線存在各頁的版面檔裡 | 改成跨頁共用後舊的讀不到。已加自動搬家（`migrateGuides`） |
| 直接用量到的寬高凍結 | 小數四捨五入後可能比內容窄一點點，收縮寬度的文字被擠到換行。修法：補半像素 |
| `say()` 的 toast 計時器直接抓外層 `toast` 變數 | 離開編輯模式後 `toast` 被清成 null，1.6 秒後計時器觸發就丟 TypeError |

### 其他曾經踩過的小坑

| 問題 | 原因 |
| --- | --- |
| 四格的圖示顏色變成暗灰而不是亮色 | `.act span` 的優先權（0,1,1）壓過 `.act__ico`（0,1,0），而 `.act__ico` 也是 `<span>`。修法：說明文字改用 `.act__sub` class |
| 全域快捷鍵在輸入欄裡照樣觸發 | 在首頁的名稱欄打 `0` 或 `1` 會直接跳頁。修法：keydown 先擋掉 input / textarea，`Esc` 在欄位裡改成離開欄位 |
| 公開 API 的 `select(key)` 沒有加選參數 | 想用程式測多選時只選得到一個。已改成 `select(key, add)` |

### 啟動流程

| 試過但失敗的做法 | 為什麼失敗 |
| --- | --- |
| 啟動鏈用 `requestAnimationFrame` 等版面穩定 | **分頁在背景或沒有合成時 rAF 不會觸發**，整條鏈卡死，版面永遠不套用、載入遮罩不撤 | 
| 只靠 `document.fonts.ready` | 字體載入失敗就永遠不 resolve。已加 4 秒保險 |

### 已刪除的死變數
`--panel` / `--panel-hi` / `--panel-solid` 都是舊版填色面板留下的，改成無填色之後全部移除。**不要再引用。**

---

## 5. 開發環境的地雷（會浪費大量時間，務必先看）

這些跟程式無關，是這台機器 / 這個工具環境的問題。前面的對話在這裡卡了非常久。

1. **開發伺服器會被系統回收**。用 `preview_start` 起來之後常常幾十秒內就死掉。
   死掉之後 headless 截圖會拿到一張 **22955 bytes 的「無法連上這個網站」PNG** —— 看到這個大小就知道是伺服器掛了，不是版面壞了。
   → 重新 `preview_start`，然後**立刻**做要做的事。

2. **PowerShell 工具是沙箱化的**。在裡面 `Start-Process` 起的 node 伺服器，外面的 Chrome 連不到。
   用 Bash + `dangerouslyDisableSandbox` 起的背景程序，工具呼叫一結束就死。
   → 伺服器只能靠 `preview_start`。

3. **Browser 預覽窗格不會合成畫面** → `requestAnimationFrame` 永遠不觸發 →
   **所有 CSS transition / animation 都不會推進**，`getComputedStyle` 讀到的是**起始值**。
   曾經因此誤判「顏色沒變」「框線沒加粗」好幾次。
   → 量之前先注入 `*{transition:none!important}`。
   → **注意 `*` 選不到偽元素**，要另外寫 `.foo::after{transition:none!important}`。

4. **切頁後舊頁的 `visibility` 卡在 visible**（同上，transition 不推進），
   舊頁會蓋在新頁上面吃掉點擊，導致「編輯模式 0/14 可選取」這種假象。
   → 量之前先注入 `.page{transition:none!important}`。

5. **headless Chrome 有時候會用 1903×984 排版再把截圖放大到 1920×1080**，看起來像整個版面被撐大／右邊被裁掉。
   → 需要精確截圖時用 `--window-size=1937,1176` 再裁到 1920×1080，或者乾脆改用數值驗證。

6. **測試殘留的 `layout*.json` 會毒化所有量測**。
   存檔優先於 CSS，一份舊的存檔會讓你以為版面壞了。前面至少誤判三次。
   → **每次量測／截圖前先刪掉 `layout*.json` 並清 localStorage。**

7. PowerShell 裡 `Remove-Item` 和 `"C:\Program Files\..."` 字串**出現在同一個指令**會被沙箱擋下
   （誤判成要刪系統路徑）。→ 拆成兩次呼叫。

---

## 6. 目前已知的問題與未完成事項

### 未完成
- ~~綠色介面 / 紅色介面兩頁還是空白骨架~~ → **0.14 已刪除這兩頁**，現在只剩第一頁和首頁。
- **首頁左下三個功能鍵（立即警報 / 逃生路線 / 安全法規）沒有任何行為**，只有 hover / focus 樣式
  （右側那兩顆「展示／切換逃生動線」是有作用的，見 0.8）
- 登入面板已經整個移除（見 0.8），現在沒有任何登入或身分的概念

### 需要使用者提供的內容
- 右側三張階段卡的說明文字、四格「正確應對」的文案 —— **目前是我照語意暫擬的**，使用者說過實際要用的字會再給
- 首頁右下的 `[新大樓名稱]` 是佔位字
- 使用者提過 `3d/0715.glb`（42MB）可以拖進模型區測試，但一直沒實際測過外部模型載入

### 懸而未決的問題（我問了但使用者沒回）
- 第一頁的**倒數圓盤中心在 x=1227，不在畫面正中**（對齊左欄的右半邊，跟原始海報一樣）。
  使用者說「置中」時我判斷是指四格那排並改好了，同時問了圓盤要不要一起置中，**沒得到回覆**
- 首頁三個功能鍵的第一顆圖示，參考圖看起來是大樓，我改用鈴鐺（配「立即警報」語意才通），有告知但沒回覆

### 潛在風險
- 版面存檔是**照編輯當下的視窗比例**存的。比例差很多（16:9 → 直式）時位置不會自己重排。
  已加防呆：套用後三成以上物件跑到畫面外就整份丟掉、回自動排版並在 console 警告
- 直式版面下 `.stage` 是 `display:none`，編輯器會跳過沒被排版到的物件（避免存成 0×0）

---

## 7. 使用者的偏好與限制

- **用繁體中文溝通**
- **改首頁時不要動到第一頁**（明講過）。做三層模型那次特別強調
- **偏好細緻、克制的視覺**：線條要求細過兩次、粗體字要求調細、明確拒絕「整張卡變亮」
- **在意對齊與置中**：多次要求置中、等距、同高、同尺寸
- **不要鍵盤提示欄**（右下角那條已經整個移除，包含 CSS 和 JS）
- 會直接指出「跑掉了」「不太正確」，但不一定講得出是哪個元素 —— **先量數字找出真正偏掉的東西，不要憑感覺猜**
  （例：說「第一頁跑掉了」，實際上頁面外框是對稱的，偏掉的是四格那排）
- 會誤以為是最近的改動造成的問題（例：模型改完說「首頁比例全亂」，其實是舊存檔）。
  **遇到這種回報先確認是不是存檔或環境問題，並把判斷依據講清楚。**

---

## 8. 建議的下一步順序

1. **請使用者確認最後一輪的視覺**（面板框發光、字重變細）—— 這是唯一沒完成驗證的部分
2. ~~補綠色 / 紅色兩頁的內容~~（0.14 已把這兩頁刪掉）
3. 首頁三個功能鍵接上行為（需要先問使用者按下去要做什麼）
4. 替換階段卡與四格的正式文案
5. 實際測一次外部 `.glb` 載入（拖曳 + `?model=`）
6. 視需要處理：倒數圓盤要不要置中、功能鍵第一顆的圖示

---

## 9. 檔案用途速查

| 檔案 | 用途 | 動它之前要知道 |
| --- | --- | --- |
| `index.html` | 四頁的版面 | 可編輯物件靠 `data-obj` / `data-label` 標記，加了就自動進編輯清單 |
| `css/style.css` | 主題變數 + 全部樣式 | 最上面是主題與 `.chamfer`（倒角面板的核心），碰之前先讀註解 |
| `css/editor.css` | 編輯模式的 UI | `.eo-*` 全部是編輯器專用 |
| `js/main.js` | 頁面路由、鍵盤、主題、登入、啟動流程 | 編輯器實例、歡迎流程參數都在這 |
| `js/viewer.js` | Three.js 場景 | 兩組佔位模型 + 視角存取 API，`SCENE_VIEWS` 在最上面 |
| `js/countdown.js` | 30 秒倒數 | 有 `onEnd` 回呼（歡迎流程用）、`urgentAt`（閃爍門檻） |
| `js/editor.js` | 版面編輯器 | 最大的檔案，凍結／拖曳／對齊／參考線／視角都在這 |
| `js/guides.js` | 跨頁共用參考線 | 獨立 store，兩個編輯器共用同一份 |
| `server.mjs` | 靜態伺服器 + `PUT/DELETE /layout*.json` | **改了要重啟才生效** |
| `README.md` | 使用說明與技術細節 | 寫得很完整，技術問題先查這裡 |
| `layout*.json` | 編輯模式存的版面／參考線 | 自動產生，已被 `.gitignore` 排除 |

---

## 10. 幾個容易忘記的技術點

- **Three.js 從 unpkg CDN 載入**（`0.169.0`，importmap 在 `index.html`），沒有 npm、沒有建置流程。直接開檔案即可，但**必須用 http 開**（ES Module 不能 `file://`）
- 啟動：`node server.mjs 5280`
- **`html` 和 `body` 都是 `overflow:hidden`** —— 全螢幕看板不能出現捲軸，因為捲軸會改變 `vw/vh` 進而改變 `rem`，整個版面會跳一下
- 網址參數：`?page=` `?theme=` `?edit=` `?layout=off` `?model=`（`?edit=<物件代號>` 會順便選取該物件，截圖很好用）
- Console 有 `__viewer` / `__countdown` / `__goto` / `__editors` / `__editor` / `__setTheme` 可直接操作
- 存檔格式：`{ objects: {代號: {x,y,w,h,s,hidden}}, view: {pos,target,autoRotate} }`，座標單位 `rem`
- 參考線格式：`{ x: [{p:'頁代號', v:rem}], y: [...] }`
- 可編輯物件數量：第一頁 **14** 個、首頁 **9** 個。驗證編輯器時拿這兩個數字對
- 物件代號：
  - first：`hero` `stage` `timerRing` `timerFoot` `actsTitle` `act1`–`act4` `sideHead` `phase1`–`phase3` `warn`
  - home：`homeBrand` `homeStage` `homeRoutePlay` `homeRouteNext` `homeAct1`–`homeAct3` `homeGuideTitle` `homeGuideSlogan`
    （`homePanel` 是舊的歡迎面板，已移除，`layout-home.json` 裡那筆也一併刪掉了）
- **git 身分只設在這個 repo 內**（`qifenwei03-del` + GitHub noreply 信箱），沒有動使用者的全域 git 設定。
  這台機器原本沒有設過 `user.name` / `user.email`

---

## 11. 驗證這個專案的建議做法

因為預覽環境不可靠（見第 5 節），驗證時**優先用數值而不是截圖**：

```js
// 在 Browser 預覽窗格裡跑，量之前一定要先關 transition
const kill = document.createElement('style');
kill.textContent = '.page{transition:none!important}';
document.head.appendChild(kill);
window.__goto('first');
// …量 getBoundingClientRect / getComputedStyle…
kill.remove();
```

要截圖時：
1. 先刪 `layout*.json`、清 localStorage
2. `preview_start` 之後**立刻**截
3. 檔案只有 ~23KB → 伺服器掛了，不是版面壞了
4. 需要精確尺寸就用 `--window-size=1937,1176` 再裁到 1920×1080
